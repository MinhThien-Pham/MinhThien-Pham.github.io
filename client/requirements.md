## Packages
framer-motion | Smooth animations for scroll reveals and interactions
react-scroll | Smooth scrolling for navigation links

## Notes
The application requires a dark, systems-programming aesthetic.
Google Fonts (JetBrains Mono, Inter) will be used via CSS imports.
Resume data is fetched from GET /api/resume.
Contact form posts to POST /api/contact.
